/**
 * Densified 3-role icon glyph sizes (Plan B).
 * CSS counterparts live in globals.css:
 *   --control-toolbar-glyph / --control-badge-glyph / --glyph-inline-size
 *
 * Boxes (CSS): toolbar 32, badge 18; inline has no box.
 */
export const ICON_GLYPH = {
  toolbar: 20,
  badge: 14,
  inline: 16,
} as const;

/** Retired: rename map targeted pre-layout paths. Kept as a no-op so old docs/CI don't explode. */
console.log("retired: verify-reorg-scope (stale rename map; layout already settled)");
process.exit(0);
